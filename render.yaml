services:
  - type: web
    name: zahari-medical
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: zahari_medical
          property: connectionString
      - key: NODE_ENV
        value: production